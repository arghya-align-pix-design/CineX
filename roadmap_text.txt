CineXSpring Boot Cinema PlatformProject Roadmap  — 
 3-Month Build PlanConstraint: 2–3 hrs/day  
 ·  Parallel project running  
 ·  Each level = deployable,
  CV-ready productTarget: YC-level system design
   portfolio piece + SDET-ready test 
   layerOverviewThis roadmap is structured 
   into 4 levels. Each level ends with a fully 
   functional, independently deployable product 
   that you can describe on your CV, demo in an 
   interview, and push to GitHub with a working
    README. You never have a half-baked project 
    sitting in your portfolio.
    LevelDeliverableDaysWeeks (~)Stop here if 
    neededLevel 1Authenticated REST API + 
    Theatre Builder182.5Strong API portfolio 
    pieceLevel 2Full Booking Engine + Payments 
    + CI/CD223Distributed systems demoLevel 
    3Loyalty Engine + ML Pricing 
    Service132AI/data layer highlightLevel 
    4Full-Stack Product + Vendor UI142Complete 
    deployable productTotal: 67 days · ~10 weeks
     @ 2–3 hrs/day. Fits inside 3 months with 
     buffer.LEVEL 1Authenticated REST API + Theatre BuilderCV: "Built a REST API for a multi-tenant cinema platform with self-serve theatre onboarding, seat schema configuration, and JWT-based role auth"18daysAt the end of Level 1 you have: a running Spring Boot API, Docker-based PostgreSQL, JWT auth with three roles, and a theatre seat-map builder where an owner can create sections, configure seat grids (stored as JSONB), and retrieve a full layout. This is a complete, documented API you can host on Railway/Render and link in your CV today.Phase 0 — Foundation & Project Setup3 days  ·  6–9 hrs totalPhaseWhat you buildDaysHrsDay 1Spring Boot init (start.spring.io). Add Web, JPA, Lombok, PostgreSQL, Validation dependencies.12–3Day 2Docker Compose: PostgreSQL + pgAdmin. Connect Spring to DB. GET /health endpoint.12–3Day 3Package structure: controller / service / repository / entity / dto. Push to GitHub. .env for secrets.12–3Tech stack — Phase 0Spring Boot 3.x — via start.spring.ioMaven — dependency managementPostgreSQL 15 — primary databaseDocker + Docker Compose — local dev env  NEWLombok — boilerplate reductionIntelliJ IDEA / VS Code — IDEGit + GitHub — version control Learning budget: 1 hr — Docker basics: running containers, docker-compose.yml syntax, mapping ports and volumes. Watch one TechWorld with Nana Docker tutorial, that is all you need.Checkpoint: GET /health returns 200. Spring connects to Dockerised PostgreSQL. Clean folder structure committed to GitHub.Phase 1 — Auth & User Service5 days  ·  10–15 hrs totalPhaseWhat you buildDaysHrsDay 1User entity: id, email, passwordHash, role (CONSUMER / VENDOR / ADMIN). UserRepository.12–3Day 2BCrypt hashing. POST /auth/register + POST /auth/login returning JWT.12–3Day 3JwtFilter — validates token on every request, sets SecurityContext.12–3Day 4Role-based access: @PreAuthorize on endpoints. CONSUMER gets 403 on VENDOR routes.12–3Day 5POST /auth/refresh token. Full Postman test collection for auth flows.12–3Tech stack — Phase 1Spring Security 6 — security filter chain  NEWJWT (jjwt library) — token generation + validation  NEWBCrypt — password hashing (built into Spring Security)Spring Data JPA — UserRepositoryLearning budget: 1 hr — Spring Security filter chain mental model. Key concept: every HTTP request passes through a chain of filters before hitting your controller. The JwtFilter sits in this chain. One blog post + one YouTube short is enough.Checkpoint: Register as VENDOR, login, get JWT. Use JWT to call POST /theatres successfully. Use CONSUMER token on same endpoint, get 403. Both verified in Postman.Phase 2 — Theatre Service & Seat Schema Builder10 days  ·  20–30 hrs totalPhaseWhat you buildDaysHrsDay 1Theatre entity, Section entity (seatType, rows, cols, priceMultiplier, seatType enum: SILVER/GOLD/RECLINER).12–3Day 2Seat layout stored as JSONB column. SeatGridConfig Java class, JSON converter.12–3Day 3TheatreService + TheatreRepository. POST /theatres (vendor creates theatre).12–3Day 4SectionService. POST /theatres/{id}/sections — add section with seat grid config.12–3Day 5GET /theatres/{id}/layout — returns full JSON seat map (used by both vendor editor and consumer seat picker).12–3Day 6PUT/PATCH for editing sections. Soft-delete seats (status = INACTIVE).12–3Day 7Validation: max seats per section, unique seat codes (A1, A2…), duplicate section name check.12–3Day 8Global exception handler: @ControllerAdvice, consistent error response schema.12–3Day 9MapStruct DTOs: TheatreResponse, SectionResponse. Never expose entities directly.12–3Day 10Postman collection: full theatre flow. Export as JSON, commit to repo. Write README.md.12–3Tech stack — Phase 2Spring Data JPA + Hibernate — ORMPostgreSQL JSONB — storing seat grid as JSON column  NEWMapStruct — DTO mapping (compile-time, not runtime reflection)  NEWPostman + Newman CLI — API testing and collection exportLearning budget: 1 hr — PostgreSQL JSONB: how to store, index, and query a JSON column. Key annotation: @Type from Hibernate Types library. One Baeldung article covers this completely.Level 1 Final Checkpoint: Create a 3-section theatre (Silver 10x5, Gold 8x5, Recliner 4x3). GET /layout returns correct nested JSON seat grid. Auth enforced on all write endpoints. README on GitHub explains the API. Deploy to Railway or Render (free tier). This is now a CV project.LEVEL 2Full Booking Engine + Payments + CI/CDCV: "Built a distributed cinema booking system with Redis-based concurrent seat locking, Razorpay payment integration, Kafka event streaming, and GitHub Actions CI/CD pipeline"22daysAt the end of Level 2 you have the complete booking core: movies, shows, seat availability, Redis-locked checkout, Razorpay payments, and a GitHub Actions pipeline that runs all tests on every push. This is what separates you from tutorial-project builders. The concurrent seat locking test alone is a conversation starter in any system design interview.Phase 3 — Movie & Show Service5 days  ·  10–15 hrs totalPhaseWhat you buildDaysHrsDay 1Movie entity: title, description, language, durationMins, genre, posterUrl. MovieRepository.12–3Day 2Show entity: movie, theatre, section, startTime, endTime, basePrice, status (UPCOMING/LIVE/COMPLETED).12–3Day 3ShowService: POST /shows (vendor creates show in their theatre for a movie).12–3Day 4GET /shows?city=&date=&movieId= — consumer search endpoint, paginated with Spring Pageable.12–3Day 5GET /shows/{id}/seats — returns seat grid with availability (AVAILABLE / IN_CHECKOUT / BOOKED).12–3Checkpoint: Vendor creates a show. Consumer searches by date and city. /seats endpoint returns grid with correct status for each seat.Phase 4 — Booking Service + Redis Seat Locking10 days  ·  20–30 hrs total  — THE HARDEST AND MOST IMPRESSIVE PHASEPhaseWhat you buildDaysHrsDay 1Add Redis to Docker Compose. Spring Data Redis config. Test basic set/get with StringRedisTemplate.12–3Day 2SeatLockService: lock(showId, seatId, userId) via Redis SETNX with 10-min TTL. unlock(). isLocked() methods.12–3Day 3POST /bookings/initiate — validates seats available, acquires Redis locks on all selected seats, returns bookingToken.12–3Day 4Lock failure handling: if any seat already locked, release all acquired locks, return 409 with seat ID list.12–3Day 5Booking entity: id, user, show, seats (JSON), status (PENDING/CONFIRMED/CANCELLED), totalPrice.12–3Day 6POST /bookings/confirm — called after payment. Writes booking to DB, releases Redis locks, marks seats BOOKED.12–3Day 7Expiry job: @Scheduled checks PENDING bookings older than 12 mins, cancels and releases locks.12–3Day 8GET /bookings/my — user booking history. GET /bookings/{id} — ticket details with QR code reference.12–3Day 9Publish BookingConfirmed Kafka event after confirmation. Add Kafka + Zookeeper to Docker Compose.12–3Day 10Write concurrent test: 10 threads race for seat A5, verify exactly ONE succeeds, DB has exactly ONE booking.12–3Tech stack — Phase 4Redis 7 — distributed seat locks via SETNX + TTL  NEWSpring Data Redis + Lettuce — Redis client  NEWApache Kafka — BookingConfirmed event publishing  NEWJUnit 5 + ExecutorService — concurrent load testLearning budget: 1 hr — Redis SETNX and TTL. The key mental model: SETNX (SET if Not eXists) is atomic. Only one thread can win the race to set a key. If the key already exists, the command returns 0 (fail). This is your seat lock. Expiry (TTL) auto-releases the lock if payment never comes.Checkpoint: Concurrent test: 10 simultaneous requests for seat A5 — exactly 1 succeeds, 9 get 409. Booking lands in DB with status PENDING. Kafka event visible in console. BookingConfirmed topic has the message.Phase 5 — Payment Integration + CI/CD Pipeline7 days  ·  14–21 hrs totalPhaseWhat you buildDaysHrsDay 1Razorpay Java SDK. POST /payments/create-order — creates Razorpay order, stores orderId in Booking.12–3Day 2POST /payments/webhook — Razorpay calls this on success. Verify HMAC-SHA256 signature. Call BookingService.confirm().12–3Day 3Payment failure path: booking cancelled, Redis locks released, seats freed. Refund stub for cancellations.12–3Day 4Full Razorpay test-mode flow. Verify webhook fires, booking confirms, seat marked BOOKED.12–3Day 5GitHub Actions: .github/workflows/ci.yml — on push: Maven build, spin up Docker Compose services, run all tests.12–3Day 6Newman (Postman CLI) step in GitHub Actions. API collection runs in CI. Badge in README.12–3Day 7@SpringBootTest integration tests for booking flow end-to-end. @Testcontainers for PostgreSQL + Redis in CI.12–3Tech stack — Phase 5Razorpay Java SDK — payment processing  NEWGitHub Actions — CI/CD pipeline, quality gates  NEWNewman CLI — Postman collections in CI  NEWTestcontainers — spin up real Postgres + Redis in tests  NEWHMAC-SHA256 — webhook signature verification@SpringBootTest — full integration test contextLevel 2 Final Checkpoint: Full flow in Razorpay test mode: initiate booking (Redis locks), pay, webhook fires, booking confirmed in DB, seats marked BOOKED. GitHub Actions runs green on every push. README has CI badge. This is now a distributed systems CV project.LEVEL 3Loyalty Engine + ML Dynamic PricingCV: "Implemented event-driven loyalty reward system using Kafka consumer groups and a Python FastAPI ML pricing service with demand-aware surge multipliers integrated into the Spring Boot platform"13daysLevel 3 adds two things that make this project stand out from every other booking-clone: an event-driven loyalty system that rewards repeat customers with Recliner seat access, and a Python microservice that dynamically prices shows based on demand signals. This is the layer that makes interviewers ask follow-up questions.Phase 6 — Loyalty Engine5 days  ·  10–15 hrs totalPhaseWhat you buildDaysHrsDay 1LoyaltyAccount entity: userId, cineScore, tier (BASIC/SILVER/GOLD/PLATINUM), reclinersUnlocked boolean.12–3Day 2Kafka consumer (Spring @KafkaListener) on booking-confirmed topic. On event: award points (price * multiplier).12–3Day 3Tier logic: if totalSpend > 2100 AND qualifyingBookings >= 6 (at Rs.350+) → unlockRecliner(). Publish LoyaltyUnlocked event.12–3Day 4GET /loyalty/me — returns score, tier, qualifying booking count, progress to unlock (e.g. 4/6), active perks.12–3Day 5Recliner unlock logic in BookingService: if seat is RECLINER type, check loyaltyService.hasReclinderAccess(userId), apply 75% discount on premium delta.12–3Tech stack — Phase 6Spring Kafka Consumer — @KafkaListener, consumer groups  NEWSpring Data JPA — LoyaltyAccount persistenceEvent-driven design — Booking → Loyalty decoupling via Kafka Learning budget: 45 min — Kafka consumer groups and offset management. Mental model: a Kafka topic is a log. Your consumer group reads from a committed offset. At-least-once delivery means your handler must be idempotent (same event processed twice should not double-award points). Use a processedEventId table.Checkpoint: Make 6 bookings at Rs.350+ via Postman. GET /loyalty/me shows tier=PLATINUM, reclinersUnlocked=true. Book a Recliner seat — API applies 75% perk discount on the premium delta. Loyalty events visible in Kafka topic.Phase 7 — Python FastAPI Pricing Service8 days  ·  16–24 hrs totalPhaseWhat you buildDaysHrsDay 1Python venv. pip install fastapi uvicorn scikit-learn pandas. Hello world FastAPI app running on port 8001.12–3Day 2POST /price endpoint. Pydantic input model: dayOfWeek, hoursUntilShow, occupancyPct, genre, isWeekend.12–3Day 3Rule-based v1: weekend +20%, evening peak +15%, occupancy > 70% +25%, action/horror genre +10%. Returns multiplier float.12–3Day 4Generate 1000-row synthetic training dataset with pandas. Feature engineering.12–3Day 5Train GradientBoostingRegressor on dataset. Evaluate RMSE. Pickle model to model.pkl.12–3Day 6Load pickled model at FastAPI startup. Swap rule-based endpoint to use ML predictions.12–3Day 7Dockerfile for Python service. Add to docker-compose.yml alongside Spring services.12–3Day 8Spring ShowService calls Python via RestTemplate (50ms timeout, fallback = 1.0x multiplier). End-to-end test.12–3Tech stack — Phase 7Python 3.11 — scripting + MLFastAPI + Uvicorn — Python microservice framework  NEWscikit-learn — GradientBoostingRegressor ML model  NEWpandas — dataset generation + feature engineering  NEWPydantic v2 — request/response validation (built into FastAPI)pickle — model serializationRestTemplate — Spring HTTP client to call Python service Learning budget: 1 hr — FastAPI basics. It is dramatically simpler than Spring Boot. Routing, Pydantic models, running with uvicorn: these are covered in the official FastAPI tutorial in under an hour. Do the first 4 sections only.Level 3 Final Checkpoint: POST /shows via Spring — pricing service is called internally, weekend evening show gets >1.2x multiplier automatically. Service times out gracefully, falls back to 1.0x. Loyalty unlock and Recliner discount work end-to-end. Python service runs in its own Docker container.LEVEL 4Full-Stack Product + Vendor UICV: "Full-stack cinema platform with React consumer seat-picker, drag-and-drop vendor theatre builder, and one docker-compose.yml that starts the entire system including Spring Boot, Python ML service, Redis, Kafka, and PostgreSQL"14daysLevel 4 completes the product. A consumer can search movies, pick seats visually, and pay. A vendor can build their theatre layout, schedule shows, and see occupancy in real time. Everything starts with one command. This is demo-ready for YC interviews, hackathons, and job applications.Phase 8 — Consumer Frontend7 days  ·  14–21 hrs totalPhaseWhat you buildDaysHrsDay 1Vite + React project. Axios config. Auth context (JWT stored in memory, not localStorage). Login/register screens.12–3Day 2Movie listing page: fetch /shows, display cards with poster image, timing, starting price, section badges.12–3Day 3Show detail page: section selector. Fetch /shows/{id}/seats. Render section tabs.12–3Day 4SeatGrid component: renders seat matrix from JSON schema. Color by status (available=green, in_checkout=yellow, taken=red). Click to select/deselect.12–3Day 5Checkout: running total, max 8 seats guard, POST /bookings/initiate, Razorpay JS popup, POST /bookings/confirm.12–3Day 6My Bookings page: booking reference, show details. Loyalty progress bar (X/6 qualifying bookings, unlock indicator).12–3Day 7Loading skeletons, error states, mobile-responsive layout. Playwright: 5 E2E test scenarios.12–3Tech stack — Phase 8React 18 + Vite — consumer frontendAxios — API calls with JWT interceptorCSS Grid — seat matrix renderingRazorpay JS SDK — frontend payment popupPlaywright — E2E test scenarios (5 flows)  NEW Phase 9 — Vendor UI + Demo Polish7 days  ·  14–21 hrs totalPhaseWhat you buildDaysHrsDay 1Vendor login + dashboard: list their theatres, upcoming shows, today's revenue.12–3Day 2Theatre creation form. Section builder: choose rows x cols, section name, seat type from dropdown.12–3Day 3Visual seat map preview: render the grid from section config so vendor sees exactly what it looks like before publishing.12–3Day 4Movie scheduling form: pick movie, date/time, assign to section. Pricing service auto-returns suggested price.12–3Day 5Occupancy view: live seat map per show showing sold/remaining seats and revenue collected.12–3Day 6Full docker-compose.yml: Spring Boot + Python FastAPI + React (nginx) + Redis + Kafka + PostgreSQL. One command starts everything.12–3Day 75-minute demo script. Fix broken flows. Write final README with architecture diagram, setup instructions, and demo walkthrough.12–3Level 4 Final Checkpoint: docker-compose up starts everything. Demo: vendor logs in, builds a 3-section theatre, schedules a show (pricing auto-set by ML service), consumer searches, picks seats (seat locking visible in Redis), pays (Razorpay test mode), booking confirmed. After 6 qualifying bookings, Recliner unlocks in the UI. The whole product lives at github.com/yourname/cineplex.SDET Additions Across All LevelsThese additions transform CineX from a developer portfolio project into an SDET portfolio project. Add them incrementally as you finish each level. They do not change the build timeline significantly but massively increase your relevance to quality engineering roles.WhenWhat to addToolsCV signalLevel 1 endExport Postman collection as JSON. Write Newman CLI script to run it. Add to GitHub Actions.Newman, GitHub ActionsAPI automation in CILevel 2 endWrite 10 JUnit 5 unit tests (service layer). Write 5 @SpringBootTest integration tests. Concurrent booking test: 10 threads race for one seat.JUnit 5, Mockito, TestcontainersDistributed system testLevel 2 endWrite Cucumber BDD feature files for booking flow. Given/When/Then format wrapping your integration tests. Use SerenityJS reporting.Cucumber, SerenityJSBDD automationLevel 3 endWrite contract tests for the Spring → Python pricing service call. Verify the API contract does not break when Python service changes.Spring MockMvc, WireMockService contract testingLevel 4 endAdd Playwright E2E suite: 10 scenarios covering full consumer flow. Add axe-core accessibility scan on seat picker page (WCAG check).Playwright, axe-coreE2E + A11y testingHow to Write This on Your CVUse these exact bullet formats. Each one is written to trigger technical screening filters at YC-stage companies and SDET roles.After Level 1Designed and built a multi-tenant cinema REST API in Spring Boot 3, featuring JSONB-backed self-serve seat schema configuration and JWT role-based access control (VENDOR / CONSUMER / ADMIN)After Level 2Implemented distributed seat locking using Redis SETNX with TTL to prevent double-booking under concurrent load; verified with a 10-thread race condition testBuilt a GitHub Actions CI/CD pipeline that runs JUnit integration tests, Testcontainers-based DB/Redis spin-up, and Newman API test suites on every pushAfter Level 3Built an event-driven loyalty reward engine using Kafka consumer groups; users unlock premium Recliner seats with 75% discount after qualifying spend thresholdsDeveloped a Python FastAPI ML microservice serving demand-aware price multipliers using a trained GradientBoostingRegressor model, integrated into Spring via REST with graceful fallbackAfter Level 4Full-stack cinema platform: React seat-picker UI, vendor theatre builder, and complete Docker Compose orchestration of Spring Boot, Python, Redis, Kafka, and PostgreSQLWrote Playwright E2E test suite (10 scenarios) including WCAG 2.1 accessibility scans using axe-core on interactive seat selection UI3-Month Timeline at a GlanceWeekFocusLevel completeParallel project impactWk 1–2Foundation, Auth, Theatre API—Low intensity phases. Safe for parallel work days.Wk 2.5Level 1 doneLevel 1Deploy to Railway. Add to CV immediately.Wk 3–4Movie/Show + Redis locking—Phase 4 needs deep focus. Block parallel work on those days.Wk 5Payment + GitHub Actions CILevel 2Level 2 done. Strongest CV update yet.Wk 6–7Loyalty + Python FastAPILevel 3Python phases are lighter. Safe to split with parallel project.Wk 8–9React frontend—Frontend days are predictable. Manageable in parallel.Wk 10Vendor UI + demo polishLevel 4Full product. YC Work at a Startup application ready.Remember: Phase 4 (Redis seat locking, concurrent test) is the single most important thing in this entire roadmap. If you only do Levels 1 and 2, you have a project that can hold its own in a system design conversation. Every phase after that is upside.